---
description: This describes the way we use Drizzle to interact with the database.
globs:
alwaysApply: false
---

# Database Schema

The database schema is defined in the `src/db/schema.ts` file.

## Tables

Each table uses a nanoId for the primary key.  They should all have a `createdAt` and `updatedAt` timestamp.